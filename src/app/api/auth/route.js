export async function GET(req) {
    return Response.json({ message: "Auth GET request successful" });
  }
  
  export async function POST(req) {
    return Response.json({ message: "Auth POST request successful" });
  }
  